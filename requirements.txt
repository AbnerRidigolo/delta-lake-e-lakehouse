# ---------------------------------------------------------------------------
# Dependencias do Data Lakehouse (Python 3.10+)
#
# Instalacao:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Observacao: no Databricks Community Edition o Spark e o Delta ja vem
# instalados no cluster. Nesse caso instale apenas o bloco "ML / AI".
# ---------------------------------------------------------------------------

# --- Engine + formato de tabela -------------------------------------------
pyspark==3.5.3
delta-spark==3.2.1

# --- Manipulacao de dados / utilitarios -----------------------------------
pandas>=2.3.3
numpy>=2.2.6
pyarrow>=25.0.0
PyYAML>=6.0

# --- ML / Feature Store ----------------------------------------------------
scikit-learn>=1.7.2
joblib>=1.5.3
# xgboost e opcional: o treinador cai automaticamente para o
# HistGradientBoostingClassifier do scikit-learn se ele nao estiver presente.
# xgboost>=2.0.0

# --- Orquestracao (opcional, apenas para rodar o DAG de verdade) -----------
# apache-airflow==2.9.3
